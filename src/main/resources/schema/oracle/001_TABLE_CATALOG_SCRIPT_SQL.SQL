CREATE TABLE CATALOG_SCRIPT_SQL (
CATALOG_ID    VARCHAR2(20 CHAR) NOT NULL,
OBJECT_TYPE   VARCHAR2(32 CHAR) NOT NULL,
OBJECT_NAME   VARCHAR2(50 CHAR) NOT NULL,
FILENAME      VARCHAR2(255 CHAR) NOT NULL UNIQUE,
ONE_TIME      CHAR NOT NULL CHECK (ONE_TIME IN ('0','1'))
);
-- Primary key
ALTER TABLE CATALOG_SCRIPT_SQL ADD CONSTRAINT PK_CATALOG_SQL PRIMARY KEY (CATALOG_ID);
